const ShowCampaigns = ({campaign}) => {
    const {id, title, image, description, status, contact_info,division } = campaign;
    return (
        <div>
            
        </div>
    );
};

export default ShowCampaigns;